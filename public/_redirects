# Retired pages. Make a Memory was folded into the philanthropy page, which now
# carries the CAN complimentary-stay detail in its own section.
/oxford/makeamemory.html  /oxford/philanthropy.html#make-a-memory  301

# The Oxford rentals page was folded into the main properties search, which
# lists the same three stays. Anchors map to the per-property cards; the gift
# card block moved to the Oxford vouchers page.
/oxford/availability.html  /search-results.html?destination=Oxford  301

# The Oxford sub-site's own reviews and sustainability pages duplicated the
# root ones the nav points at, and index2.html was its homepage, orphaned once
# every sub-site logo was repointed at /oxford. All three now forward.
/oxford/reviews.html         /oxford-reviews.html         301
/oxford/sustainability.html  /oxford-sustainability.html  301
/oxford/index2.html          /oxford.html                 301

# A Webflow template shell with an empty body, never linked and never in the
# sitemap. The properties search is what it was a stub for.
/detail_properties.html  /search-results.html  301

# The 3D tour landing pages. The tour itself is hosted off-site and linked from
# the Oxford page, so these go there rather than straight out to another domain.
/oxford/vr.html           /oxford.html  301
/oxford/vr2.html          /oxford.html  301
/oxford/copy-of-map.html  /oxford.html  301

# The shop and its two product pages. Neither ever had a checkout behind it, so
# this is a closure rather than a pause; the honey and the chocolate keep their
# own editorial pages, which is where each product page points.
/oxford/shop.html             /oxford.html            301
/oxford/product-honey.html    /oxford/honey.html      301
/oxford/product-bonbons.html  /oxford/chocolate.html  301

# Vouchers, both woodlands. Not on sale at the moment and due to return as part
# of booking a stay, so both point at the retreats rather than at a dead end.
# (The Oxford one is copy-of-facilities.html: a Webflow duplicate whose title
# was changed to "Vouchers" but whose filename never was.)
/oxford/copy-of-facilities.html  /search-results.html?destination=Oxford  301
/dorset-gift-certificates.html   /search-results.html?destination=Dorset  301

# The Klevio app instructions. Check-in now runs through the guest web-app,
# which the Oxford FAQ explains and links, so that is where this goes.
/oxford/klevio.html  /oxford/faq.html  301
